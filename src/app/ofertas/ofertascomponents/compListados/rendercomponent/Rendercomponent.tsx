import { FC } from 'react';

const Rendercomponent: FC = () => {
  return (
    <div className='bg-gray-50 shadow-lg h-full'>
                Render component here
    </div>

  );
};

export default Rendercomponent;