import React, { FC } from 'react';

interface RenderElementsProps {
  
}

const RenderElements: FC<RenderElementsProps> = ({ }) => {
  return (
    <div>RenderElements</div>
  );
};

export default RenderElements;